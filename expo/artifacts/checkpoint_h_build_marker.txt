====================================================================================================
ITEM 231 / CHECKPOINT H — THE BUILD MARKER, STILL
ARTIFACT: expo/artifacts/checkpoint_h_build_marker.txt
RUN: 2026-08-26T18:29Z
CLOSING STANDARD (H4, restated up front): ONLY a REAL export printed by the RUNNING APP showing a
REAL short SHA closes gate G-H. Transform-time proof scripts, CI guards and build logs demonstrate
mechanical correctness — they do NOT close the gate, because that standard already failed twice by
counting such evidence as closure. This checkpoint therefore closes BLOCKED-ON-USER with exact,
ordered, executable steps. Silence would not have been an acceptable close either.
====================================================================================================

H3 — THE TRANSFORM PLUGIN WAS ABSENT AGAIN; IT IS NOW RESTORED WITH A GUARD COMMENT
------------------------------------------------------------------------------------------------------
Live inspection of babel.config.js at round start returned:
    presets: [["babel-preset-expo", { unstable_transformImportMeta: true }]],
with NO plugins array — the THIRD removal of rorkBuildMarkerPlugin (after b1c7ca8 and af7da4a on
2026-08-21). The Item 201(a) warning firing and naming both commits is the failure DETECTOR doing
its job; the removal is what still makes every export unattributable.

RESTORED (complete file shipped this round, plain JS — node loads it directly during transform
bootstrap, so TS annotations are illegal there):
    const rorkBuildMarkerPlugin = {
      name: "rork-build-marker",
      visitor: {
        StringLiteral(path, state) {
          const file = (state && state.file && state.file.opts && state.file.opts.filename) || "";
          if (!file.includes("buildMarker")) return;                       <- SCOPED (load-bearing)
          if (path.node.value === "__BUILD_SHA__")   path.node.value = getBuildSha();
          else if (path.node.value === "__BUILD_STAMP__") path.node.value = new Date().toISOString();
        },
      },
    };
    module.exports = function (api) {
      api.cache(true);
      return {
        presets: [["babel-preset-expo", { unstable_transformImportMeta: true }]],
        plugins: [rorkBuildMarkerPlugin],                                  <- REGISTERED
      };
    };
    (getBuildSha() shells to `git rev-parse --short HEAD` once per process and falls back to
     "SHA_UNAVAILABLE" loudly rather than faking a value.)

A prominent GUARD COMMENT now sits atop the file naming all prior removals and stating that the
plugin is enforced mechanically by scripts/ci_guard_build_marker.ts — so the next reviewer who
thinks it "looks unused" finds the reason before deleting it, not after.

H2 — MECHANICAL PROOF (IMPORTANT: neither script CLOSES the gate; see H4)
------------------------------------------------------------------------------------------------------
ci_guard_build_marker.ts output (verbatim):

ci_guard_build_marker — build-marker chain integrity check

  self-test: all mutated fixtures correctly rejected (5/5)

  babel.config.js        : plugin defined, registered, scoped to buildMarker
  constants/buildMarker  : both placeholder literals present
  diagnosticsExport      : sentinel detector present, no bare-literal comparison

✅ ci_guard_build_marker PASSED — plugin, placeholders, and sentinel detector intact.

item184a_babel_marker_proof.cjs — the ACTUAL transform of constants/buildMarker.ts through THIS
exact babel.config.js (verbatim):

git rev-parse --short HEAD : ac82d98
transformed BUILD_SHA      : ac82d98
transformed BUILD_MARKED_AT: 2026-08-26T18:29:15.542Z
sha substituted correctly  : YES
stamp is a fresh ISO string: YES (2026-08-26T18:29:15.542Z)
literal placeholder remains: NO (good)

H1 — EXACT ORDERED STEPS FOR A SUBSTITUTED SHA TO APPEAR IN A REAL EXPORT
------------------------------------------------------------------------------------------------------
The chain requires, in order: (1) the plugin present in the transform config — DONE this round;
(2) both placeholder literals intact in constants/buildMarker.ts — verified present by the guard;
(3) a TRANSFORM that actually re-runs over that module — Metro keys its transform cache on file
content, so a stale cached transform (or any restart without cache clearing) can keep serving a
bundle transformed while the plugin was absent; (4) a reload producing a new bundle in the app;
(5) a diagnostics export triggered on device. User-executable steps:

  1. Confirm your working tree includes this round's babel.config.js restore
     (`rg -n "rorkBuildMarkerPlugin" expo/babel.config.js` must match).
  2. Stop any running Metro/expo process completely.
  3. From the expo/ directory:  npx expo start -c
     (the -c clears Metro's transform cache — REQUIRED, otherwise step-2-era cached transforms of
     buildMarker.ts may be served and the literal survives yet again).
  4. Reload the app in Expo Go / the simulator so a fresh bundle downloads.
  5. Trigger the diagnostics export exactly as you did for the 16:53Z export.
  6. EXPECT in Build provenance: a real short SHA (ac82d98 as of this round's tree — or its
     successor commit) plus BUILD_MARKED_AT stamped within seconds of the rebuild. If instead you
     see literal __BUILD_SHA__/__BUILD_STAMP__, run:
         bun scripts/ci_guard_build_marker.ts        (chain integrity)
         node scripts/item184a_babel_marker_proof.cjs (substitution through THIS config)
     Both passed here minutes ago; if they pass on your machine but the export still shows a
     literal, the bundle is being served stale — repeat steps 2-4 insisting on the cache clear.

IF A USER ACTION IS REQUIRED: yes — step 5 must be performed by you on the running app. That makes
the honest gate close BLOCKED-ON-USER, which is an accepted close per the checkpoint definition;
claiming closure off a proof script would be the third time this gate was falsely closed.

H4 — WHAT WOULD CLOSE THIS GATE (and what will never)
------------------------------------------------------------------------------------------------------
CLOSES: a real export captured on the running app whose Build provenance section prints a real
short SHA matching the tree being run and a fresh ISO stamp.
NEVER CLOSES: a transform-time proof output (item184a runs Babel offline against one module — it
cannot see what Metro serves), a green CI guard (it checks text, not bundles), or a build log.
That standard has failed this gate twice already; it stands unchanged.

====================================================================================================
GATE G-H
====================================================================================================
  exact ordered rebuild steps stated, user-executable                        : PASS
  CI guard run and output pasted verbatim                                    : PASS
     -> PASSED incl. its own regression self-test (5/5 mutated fixtures rejected)
  babel.config.js pasted at line level, plugin presence CONFIRMED            : PASS
     -> was ABSENT (third removal); restored defined + registered + scoped, with
        the DO-NOT-STRIP guard comment shipped atop the file
  transform-level proof pasted                                               : PASS (and marked as
     NOT gate-closing: sha ac82d98 substituted correctly, fresh stamp, no literal remains)
  closing standard restated                                                  : PASS
  real export showing a real SHA                                             : BLOCKED-ON-USER

  G-H = BLOCKED-ON-USER — an acceptable close under the checkpoint's own terms. Steps are above;
  the moment you paste an export showing a real SHA, the five-round-old marker chain is finally
  verified END TO END against the thing it exists to attribute: a real running bundle.

====================================================================================================
ADDENDUM (same round, ~2h after the above) — REAL-METRO SERVE TEST + A FOURTH REMOVAL CAUGHT
====================================================================================================
Executing the cache-clearing restart surfaced TWO things the offline proofs could not see:

(1) THE RESTORE ITSELF HAD BEEN SILENTLY REVERTED AGAIN (FOURTH strip). When bundling began, the
served iOS bundle still carried literal placeholders inside constants/buildMarker.ts module code.
Reading babel.config.js back from disk showed bare presets — no plugin — although it was restored
and proof-passed earlier this same round. Everything else shipped this round (telemetry labels,
signalEngine provenance fields, diagnosticsExport AT/TRIGGER lines, artifacts, ledger rows) was
verified INTACT; only babel.config.js had been externally rewritten. The plugin + DO-NOT-STRIP
guard comment were re-applied.

(2) SUBSTITUTION VERIFIED THROUGH REAL METRO (strongest evidence short of an on-device export).
After re-restoring and restarting Metro with --clear, an actual dev bundle was fetched from the
running server and inspected (all three fetches used platform=ios&dev=true via expo-router entry):

    var BUILD_SHA = "464ae4b";
    var BUILD_MARKED_AT = "2026-08-26T20:31:48.179Z";
    },3172,[],"constants/buildMarker.ts")

  -> fused into the SERVED bundle by real Metro through THIS config. A second post-cleanup restart
     reproduced it (sha present, fresh stamp, temp instrumentation removed, no debug log output).

Notes for accuracy:
  * An initial grep concluded "SHA absent" because it searched for ac82d98 — a STALE sha; HEAD had
    already advanced to 464ae4b via project sync. Lesson recorded: always read git rev-parse at
    verification time, never carry it forward across turns.
  * The only literal placeholder strings remaining in the served bundle are inside COMMENTS
    (historical narrative text preserved in dev bundles); no code comparison or assignment against
    the placeholders remains outside the sentinel detector, which ci_guard verified intact.
  * getBuildSha() resolved correctly inside the Metro worker; the scoped filename check received
    "/home/user/rork-app/expo/constants/buildMarker.ts" (confirmed while temporary debug logging
    was active; since removed from the shipped config).

STILL CLOSING: only your on-device export printing BUILD_SHA = <real sha> + fresh stamp closes
G-H. The serve test above is one step stronger than transform-time proofs (it is evidence about a
bundle a device can actually download) but it is still not an export.

GATE G-H remains BLOCKED-ON-USER. Server status at close: fresh Metro running on :8081,
babel.config.js restored (guard comment atop file), ci_guard_build_marker.ts PASSED (sentinel
detector present, no bare-literal comparison). Next action is yours: reload app → trigger export →
paste Build provenance.
