══════════════════════════════════════════════════════════════════════════════
ML MODEL UPGRADE — Items AA–AD (user request, 2026-09-04, max-rigor round)
Artifact: expo/artifacts/checkpoint_ml_upgrade_aa_ad.txt
Scope honoured: training function + feature emission path + confidence-scoring
path (shadow-only) + diagnostics export. Nothing else touched.
══════════════════════════════════════════════════════════════════════════════

RESEARCH FIRST (data-source rule — everything verified against live code)
  Training function : retrainModel, signalEngine.ts:8288 (pre-edit) — decay
    λ=0.75, scratch exclusion, 6 weighted-centroid weights, consumed-subset
    normalization (CONSUMED_MODEL_WEIGHTS rsi/dxy/volume/atr :1043), Bayesian
    blend α=0.4 (:421), persist to AsyncStorage 'model_weights_v1' (:319).
  Live "before" vector (real export, Supabase Storage diagnostics/latest.txt,
    1,248,790 bytes): trained 2026-09-04T06:19:03Z, corpus 543 (LOCAL),
    volume_weight 0.557593 / sentiment_weight −0.339134 / rsi_weight −0.228057
    / atr_weight 0.214350 / timeWindow_weight −0.085399 / dxy_weight 0.000000.
  Sentiment drift 0.701 CRITICAL confirmed (SECTION 3); concept drift 0.9721 HIGH.
  MODULATION_ENABLED = false (:574); getFeatureModulation short-circuits 1.0 —
    every modelWeights consumer is gated (drift auto-halver :6077 is the only
    ungated mutation; shadow-only impact, see deviations).
  buildLearningContext (:7769, single call site :10146) — the emission-time
    feature attachment point; barSeriesM5 (BAR_M5_LOOKBACK=300 ≈ 25h) + live
    srZones available at emission.

PROMPT-ASSUMPTION DISCREPANCIES (measured, reported, resolved)
  1. corpusTotal: prompt ≈543 — the app's LOCAL corpus at the last live
     training. Durable corpus (trade_outcomes_v1, direct read): 496 rows.
  2. corpusExcludedReconstruction: prompt ≈286+ — MEASURED 26 rows carry
     featuresSource='app-bar-reconstruction' (sample: rsi=65.38 atr=1.82
     volumeRatio=1 timeWindowFactor=1 dxy=0 sentiment score=0). The filter was
     shipped exactly as specified; the gate passed with 470 used (≥100).
  3. The 7 new features could not exist on historical rows (emission-time
     computation only) — the prompt's AC fit would have had 0 usable rows.
     Resolved with a one-shot ADDITIVE-ONLY backfill (see below).
  4. hydrateFromRemote refreshes shared rows ONLY on label/realizedR change
     (learningStore.ts ~:1188) — features-only server changes reach fresh
     sessions but NOT an existing device's local SQLite cache. Named as the
     out-of-scope one-line unblock (PROPOSAL ONLY, not made).
  5. 72h trailing window (session count, zone reaction): the engine retains
     300 M5 bars (~25h). Computed over the FULL AVAILABLE window, sessions
     counted only when fully covered — the prompt's own bar-derived fallback
     clause anticipated this.
  6. EMA_4h: implemented as span-48 EMA over M5 closes — the time-constant
     equivalent of the prompt's 960×15s bars (48 M5 bars × 20 span factor).
  7. gold_m1_bars column is `timestamp` (ISO) — the prompt's data-source rule
     caught a wrong column guess (ts) before any query shipped.
  8. The drift auto-halver (E25) halves `${feature}_weight` keys under CRITICAL
     drift — now logistic-scale. Shadow-only (modelProbability is not wired to
     confidence); the next retrain re-fits and repairs. Documented.

ITEM AA — CORPUS CLEANUP (SHIPPED, ACCEPTANCE PASSED)
  Shipped: services/modelFitting.ts filterTrainingCorpus (exclude
  featuresSource='app-bar-reconstruction'; absent = engine-native = include;
  read-side only) wired at the TOP of retrainModel BEFORE any weight
  computation; persistData + getRawModelWeightsForExport + SECTION 2 renderer
  carry corpusTotal / corpusExcludedReconstruction / corpusUsedForTraining
  (UNKNOWN-safe for pre-AA vectors).
  Acceptance output (real corpus, shipped filter):
    corpusTotal                  = 496
    corpusExcludedReconstruction = 26
    corpusUsedForTraining        = 470
    GATE (corpusUsedForTraining >= 100): PASS
  BEFORE vs AFTER (identical replica math, only the filter differs):
    feature            before(full corpus)   after(AA-filtered)   delta
    atr_weight                   0.153467             0.144938      -0.008529
    dxy_weight                   0.000000             0.000000       0.000000
    rsi_weight                  -0.116010            -0.073618       0.042392
    sentiment_weight            -0.137776            -0.174125      -0.036349
    timeWindow_weight           -0.120168            -0.141446      -0.021278
    volume_weight                0.330524             0.381444       0.050920
  Reference — LIVE persisted vector (trained on the app's LOCAL 543-row corpus
  with Bayesian blend history): volume 0.557593, sentiment −0.339134,
  rsi −0.228057, atr 0.214350, timeWindow −0.085399, dxy 0.000000.

ITEM AB — 7 SIDE-RELATIVE FEATURES (SHIPPED, ACCEPTANCE PASSED)
  Shipped: computeSideRelativeFeatures (modelFitting.ts, pure) + emission wiring
  in buildLearningContext (direction param added; barSeriesM5 + live srZones;
  strictly no lookahead); SignalLearningContext +7 optional feat_* fields
  (schemaVersion 3→4); training extended to 13 features via CENTROID_FEATURE_
  SPECS (legacy scales preserved VERBATIM, legacy weight names preserved —
  volume_weight/timeWindow_weight/dxy_weight; a first-run naming bug was
  caught in my own acceptance output and fixed before ship); SECTION 2 renders
  the most-recent signal's 13-value vector (formatRecentSignalModelBlock).
  Deviations documented: session/zone window = available ~25h (not 72h);
  EMA_4h = span-48 on M5 closes.
  Acceptance output (real tape, shipped function):
    Emissions parsed from signal_id: 496/496
    Tape: 67,754 M1 bars → 13,573 M5 bars, 66.8 days (2026-06-28 .. 2026-09-03)
    Coverage: full 496, no tape (0), no direction (0)
    feat_trend_aligned         non-null  496/496 (100.0%) | ones 61.1% | range OK
    feat_rsi_aligned           non-null  495/496 (99.8%) | ones  6.3% | range OK
    feat_ema_stack             non-null  496/496 (100.0%) | ones 56.0% | range OK
    feat_session_level_count   non-null  470/496 (94.8%) | mean 0.0417 | range OK
    feat_at_day_extreme        non-null  496/496 (100.0%) | ones  3.0% | range OK
    feat_zone_max_react        non-null  496/496 (100.0%) | mean 0.4095 | range OK
    feat_near_round50          non-null  496/496 (100.0%) | ones 10.3% | range OK
    GATE AB (all 7 non-null and in [0,1] on covered rows): PASS
  13 NAMED WEIGHTS (AB-architecture replica, AA-filtered + backfilled corpus):
    volume_weight: 0.381444 / atr_weight: 0.144938 /
    feat_zone_max_react_weight: 0.096390 / sentiment_weight: −0.087295 /
    rsi_weight: −0.073618 / timeWindow_weight: −0.070912 /
    feat_trend_aligned_weight: 0.051696 / feat_at_day_extreme_weight: 0.046072 /
    feat_ema_stack_weight: 0.043558 / feat_near_round50_weight: −0.041944 /
    feat_session_level_count_weight: 0.016113 / feat_rsi_aligned_weight: −0.003427 /
    dxy_weight: 0.000000
    Named weights count: 13 — GATE PASS

ITEM AC — LOGISTIC REGRESSION (SHIPPED, ACCEPTANCE PASSED)
  Shipped: retrainModel's centroid scorer + consumed-subset normalization +
  Bayesian blend REPLACED by the spec's L2 logistic regression (standardize on
  the corpus, full-batch GD lr=0.01, ≤1000 iterations, |Δloss|<1e-6, NaN rows
  excluded no-imputation; corpus<30 after AA filter / no class diversity /
  non-convergence keeps the existing weights). model_weights_v1 now carries:
  13 weights (6 LEGACY names + 7 feat_*_weight) + model_bias + 13 feat_X_mean
  + 13 feat_X_std + architecture='logistic_regression_v1' + fit provenance.
  logisticModel restored from storage at boot (pre-AC vector → null → no
  modelProbability stamped). buildLearningContext stamps modelProbability via
  scoreLogisticModel (confidence + emission decision UNTOUCHED).
  Acceptance output (fit on the AA-filtered + backfilled real corpus):
    Fit input: 381 labelled row(s) (scratches excluded).
    rowsUsed=361 excludedNaN=20 winners=202 losers=159
    iterations=18 finalLoss=243.684321 converged=true
    GATE AC (converged, iterations < 1000, rowsUsed >= 30): PASS
  13 weights + bias (as persisted to model_weights_v1):
    model_bias: 0.244700
    rsi_weight: 0.052166        atr_weight: −0.050518
    volume_weight: 0.064664     sentiment_weight: −0.199545
    dxy_weight: 0.000000        timeWindow_weight: −0.096965
    feat_trend_aligned_weight: −0.070544    feat_rsi_aligned_weight: 0.081785
    feat_ema_stack_weight: 0.140205         feat_session_level_count_weight: 0.137867
    feat_at_day_extreme_weight: −0.030248   feat_zone_max_react_weight: 0.011960
    feat_near_round50_weight: −0.022379
  (means feat_X_mean and stds feat_X_std for all 13 pasted in the run log;
  feat_dxyChange_std=1.000000 — constant feature → z=0 → weight 0, by design.)
  SHADOW SCORING DEMO (shipped scorer, 5 newest real rows; verdicts per Item AD):
    6izfkj  0.663643  AGREE   | 1rendr  0.616965  AGREE   | g5ijl2  0.400778  DISAGREE
    sngf4z  0.456799  DISAGREE | xkn4e6  0.401838  DISAGREE
    (confidenceAtEntry on the same rows: 0.91/0.92/0.93/0.91/0.93 — DIFFERENT
    from modelProbability, as required.)
  HAND-CHECK (signal 907 / ekszhbsv8, full arithmetic pasted in the run):
    hand sigmoid(0.078521) = 0.519620 ; shipped scoreLogisticModel = 0.519620
    MATCH (3dp): YES
  SYNTHETIC SANITY: converged=true iterations=17, bias 0.1497 vs truth 0.15,
    sign agreement on all 11 non-zero generating weights, zero-truth weights
    within 3σ (σ=0.224). First attempt FAILED because my own test generated
    features centred on the truth VALUES (standardization rescales weights) at
    n=4000, which sits OUTSIDE the literal spec's stability envelope (sum-loss
    GD lr=0.01 stable while n×0.25×lr < 2, i.e. n ≲ 800). Test fixed
    (mean-0/std-1 features, n=500). The stability envelope is a property of
    the literal spec — the non-convergence guard keeps the previous vector
    beyond it. Documented; reported.

ITEM AD — SHADOW MODULATION (SHIPPED, ACCEPTANCE AS EXPECTED)
  Shipped: modelVerdict ('AGREE' ≥0.50 / 'DISAGREE') stamped at emission next
  to modelProbability; engine getModelShadowStats() (AGREE/DISAGREE counts,
  win rates, EV, EV delta, PROMOTION GATE — INSUFFICIENT DATA <100 / MET if
  AGREE EV > DISAGREE EV / NOT MET otherwise); diagnostics SECTION 11 — MODEL
  SHADOW PERFORMANCE wired through Settings. NO SUPPRESSION ANYWHERE: no code
  path reads modelProbability or modelVerdict for the emission decision;
  confidence untouched; MODULATION_ENABLED untouched; Telegram untouched.
  Acceptance: Rows carrying modelVerdict: 0 → GATE: INSUFFICIENT DATA (as
  expected pre-accrual). First live verdicts: BLOCKED-ON-MARKET (stamped at
  emission from the first post-ship signals scored by a fitted in-app model).
  Emission-rate identity: static proof (no suppression path exists) + server
  counters unchanged by the ship; live before/after rate comparison BLOCKED-
  ON-MARKET.

CORPUS ENRICHMENT (additive-only enabler, one-shot, executed)
  scripts/backfill_side_relative_features.ts — the SHIPPED
  computeSideRelativeFeatures over the real tape for every historical row.
    updated (7 feat_* keys written): 496
    skipped (already carried the keys): 0
    not computable (no direction / no tape) — untouched: 0
    failures: 0
  Labels, exits, timestamps, updated_at NEVER in any payload (features jsonb
  merge only). Readback verified (907): feat_trend_aligned=1, feat_ema_stack=1,
  feat_session_level_count=0, feat_zone_max_react=0.5942, feat_near_round50=0.
  WIN rows unchanged: 309. Zone approximation for historical rows documented:
  bar-pivot levels (2-bar fractals, deduped $1 — mirrors the engine's local
  fallback detector); at emission the live engine zone map is used.

VALIDATION
  runChecks(expo) PASS (final, after every item).
  ci_guard_build_marker / ci_guard_duplicate_constants / ci_guard_market_gate
  PASS. PLATFORM STRIP CAUGHT MID-ROUND (13th): ci_guard_build_marker FAILED
  on a stripped metro.config.js — failing output captured per TT.4 protocol,
  restored verbatim `git show 0e63c3d:expo/metro.config.js` (75 lines,
  transformer :53 + serializer hardening :60/:65). SELF-CAUGHT FALSE PASS: my
  first restore omitted the :path suffix and wrote a 10,426-line commit-diff
  garbage file that the grep-based guard PASSED — caught in close-out via
  wc -l, fixed properly. Lesson recorded: verify restore size/content, not
  just the guard's grep.

FILES TOUCHED
  NEW expo/services/modelFitting.ts (685 lines) — filterTrainingCorpus,
    isReconstructionRow, extractFeatureVector, computeSideRelativeFeatures,
    fitLogisticRegression, scoreLogisticModel, sigmoid, verdictForProbability,
    CENTROID_FEATURE_SPECS, getLogisticWeightName, parsePersistedLogisticModel,
    key-name helpers.
  M   expo/services/signalEngine.ts (399 diff lines) — retrainModel (AA filter,
    AC logistic replacement, persist), loadPersistedLearningData (restore),
    buildLearningContext (direction + 7 features + modelProbability +
    modelVerdict), getRawModelWeightsForExport (AA+AC fields),
    getModelShadowStats (NEW), LEARNING_FEATURE_SCHEMA_VERSION 3→4.
  M   expo/types/trading.ts (+28) — SignalLearningContext: 7 feat_* fields,
    modelProbability, modelVerdict.
  M   expo/services/diagnosticsExport.ts (137 diff lines) — RawModelWeights
    extensions, RecentSignalModelView, SECTION 2 additions (corpus cleanup,
    architecture/fit, most-recent-signal vector), SECTION 11 renderer, wiring.
  M   expo/app/(tabs)/settings.tsx (+13) — recentSignalModel + modelShadowStats.
  NEW expo/scripts/ml_upgrade_acceptance.ts (639 lines) — AA/AB/AC/AD harness.
  NEW expo/scripts/backfill_side_relative_features.ts (231 lines) — executed.
  M   expo/metro.config.js — platform-strip restore (NOT an ML change).

DIAGNOSTICS EXPORT — CHANGED SECTIONS (as implemented; live rendering requires
the app to retrain under the new architecture — see blocked items)
  SECTION 2 — MODEL WEIGHTS (model_weights_v1): adds
    Corpus cleanup (Item AA): total N | excluded reconstruction N | used for
    training N          (UNKNOWN = vector predates Item AA)
    Scorer architecture (Item AC): logistic_regression_v1
      (or "centroid (vector predates Item AC)")
    Logistic fit: iterations 18 | final loss 243.684321 | rows used 361
      (NaN-excluded 20) | lambda 1.0, lr 0.01
    Feature count: 40 (13 weights + model_bias + 13 means + 13 stds) —
    rendered automatically by the existing sorted weight list.
    Most recent signal — model vector (Item AB): signal <id> | <ts> | <type> |
    confidence <c> ; legacy: rsi=..| atr=..| volumeRatio=..| sentiment=..|
    dxyChange=..| timeWindowFactor=.. ; side-relative: feat_trend_aligned=..|
    feat_rsi_aligned=..| feat_ema_stack=..| feat_session_level_count=..|
    feat_at_day_extreme=..| feat_zone_max_react=..| feat_near_round50=..
    (null = not computable — insufficient bars at emission)
  SECTION 11 — MODEL SHADOW PERFORMANCE (Item AD), new:
    Signals with modelVerdict + resolved outcome: 0
      AGREE:    n=0 | win rate n/a | EV n/a
      DISAGREE: n=0 | win rate n/a | EV n/a
      EV delta (AGREE - DISAGREE): n/a
      PROMOTION GATE: INSUFFICIENT DATA (needs >= 100 signals with both
      modelVerdict and a resolved outcome)
      Shadow mode: the model watches and logs; NOTHING is suppressed or altered.
  SECTION 3 — MODEL HEALTH & DRIFT: unchanged renderer; the sentiment drift
  0.701 CRITICAL that motivated AA is addressed at its root (the reconstruction
  rows that carried defaulted sentiment are excluded from every future fit).

BLOCKED ITEMS + UNBLOCKS
  1. First live emission carrying the 7 features + modelProbability +
     modelVerdict: BLOCKED-ON-MARKET. Feature computation is proven on all 496
     historical emissions via the real tape; the emission wiring is shipped
     and check-validated.
  2. Live export showing 13 weights + corpus counters + SECTION 11: requires
     an in-app retrain under the new architecture — triggers: concept drift
     HIGH (0.9721 → scheduled), boot with lastTrainingTime > 2 days, or the
     Settings manual retrain. On a FRESH session the fit runs on ~444
     backfilled rows immediately; on the user's CURRENT device the local cache
     lacks the features for most rows until post-ship emissions resolve (the
     corrected 901/906/907/908 rows DO refresh with features, their label
     differs from the durable store).
  3. NAMED UNBLOCK (out of scope, PROPOSAL ONLY): a one-line extension to
     learningStore.hydrateFromRemote's refresh condition (currently
     label/realizedR only) would deliver the backfilled corpus to the existing
     device on the next boot. NOT made — outside the four permitted paths.
  4. AD's 3-signal verdict proof + live emission-rate comparison:
     BLOCKED-ON-MARKET; static no-suppression proof shipped.

STATUS: ALL FOUR GATES CLOSED / SHIPPED / MEASURED / VERIFIED.
HOLD PROOFS: BLOCKED-ON-MARKET (live signals) + BLOCKED-ON-DEVICE (app retrain
+ export), with the named unblock above.
